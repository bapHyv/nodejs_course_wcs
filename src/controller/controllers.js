const wildersController = require("./wilders")
const skillsController = require("./skills")
const ratingController = require("./rating")

module.exports = {
    wildersController,
    skillsController,
    ratingController
}